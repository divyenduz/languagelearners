kind: Ingress
apiVersion: networking.k8s.io/v1beta1
metadata:
  name: lingoparrot
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web

spec:
  rules:
    - host: api.languagelearners.club
      http:
        paths:
          - path: /
            backend:
              serviceName: lingoparrot
              servicePort: 80
